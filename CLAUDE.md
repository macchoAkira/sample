# CLAUDE.md

Guidance for AI assistants (Claude Code and others) working in this repository.

## Repository status

This repository (`macchoAkira/sample`) is currently **empty** — it contains no
application code, build configuration, or history yet. This file is a starting
scaffold. **Update it as soon as real code lands** so it reflects the actual
structure, tooling, and conventions of the project.

When the codebase exists, replace the placeholder sections below with concrete,
verified details (commands you've actually run, directories that actually
exist). Do not document tooling or workflows that aren't really present.

## Project overview

_To be filled in._ Describe what this project does, who it's for, and the
high-level architecture once code exists.

## Codebase structure

_To be filled in._ Outline the top-level directories and what each contains,
e.g.:

```
src/        Application source
tests/      Test suite
docs/       Documentation
```

## Development workflows

_To be filled in once the toolchain is known._ Document the real commands, for
example:

- **Install dependencies:** `<command>`
- **Run the app:** `<command>`
- **Run tests:** `<command>`
- **Lint / format:** `<command>`
- **Build:** `<command>`

## Conventions

_To be filled in._ Capture language/version, code style, naming conventions,
commit message format, and any project-specific patterns to follow.

## Git workflow

- The default development branch for AI-assisted work is
  `claude/claude-md-docs-7j2wpm` (per the current task configuration).
- Push with `git push -u origin <branch-name>`.
- Do **not** open a pull request unless explicitly asked.
- Write clear, descriptive commit messages.

## Notes for AI assistants

- Keep this file current: when you change build steps, add tooling, or
  establish a convention, update the relevant section here in the same change.
- Prefer verifying commands by running them over copying from memory.
